# Render पर deploy करने के बाद इन्हें Environment Variables में set करें

# आपकी Texly site का origin (CORS के लिए)
ALLOWED_ORIGIN=https://www.texlyonline.in,https://texlyonline.in

# Port (Render automatically set करता है — इसे change मत करें)
PORT=10000
